import { FrameContentDirective } from './frame-content.directive';

describe('FrameContentDirective', () => {
  it('should create an instance', () => {
    const directive = new FrameContentDirective();
    expect(directive).toBeTruthy();
  });
});
