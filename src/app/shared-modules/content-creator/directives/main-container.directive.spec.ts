import { MainContainerDirective } from './main-container.directive';

describe('MainContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new MainContainerDirective();
    expect(directive).toBeTruthy();
  });
});
